package airtesting.wekautil.arff;

//import weka.core.Instance;

import java.io.BufferedWriter;
import java.io.FileWriter;
import java.io.IOException;
import java.util.Map;

//import weka.core.*;

public class CreateArtificialARFF {

	/*
	 * private Instances relation; Instance intanceData; private FastVector
	 * vOperatores; private Attribute killedPercentage; private Attribute
	 * operator; FastVector attributes;
	 */

	/*
	 * public void creatorRelation(String nomeRelação, Map<String, Double> data)
	 * {
	 * 
	 * // Define dados da relação attributes = new FastVector();
	 * 
	 * // instancia lista de operadores this.vOperatores = new FastVector(); for
	 * (String keyKilled : data.keySet()) {
	 * this.vOperatores.addElement(keyKilled); }
	 * 
	 * // Criando lista this.operator = new Attribute("Operator", vOperatores);
	 * // Percentual de mortos this.killedPercentage = new Attribute(
	 * "Percentual Mortos");
	 * 
	 * this.attributes.addElement(this.operator);
	 * this.attributes.addElement(this.killedPercentage);
	 * 
	 * // Define relação this.relation = new Instances(nomeRelação, attributes,
	 * 0);
	 * 
	 * for (String keyKilled : data.keySet()) { this.intanceData = new
	 * Instance(this.relation.numAttributes());
	 * this.intanceData.setValue(this.operator, keyKilled);
	 * this.intanceData.setValue(this.killedPercentage, data.get(keyKilled));
	 * this.relation.add(this.intanceData); }
	 * 
	 * this.writerARFF(this.relation); }
	 * 
	 * public void writerARFF(Instances relation){ BufferedWriter writer; try {
	 * //verificar se o diretótio e o arquivo existem writer = new
	 * BufferedWriter(new FileWriter("C:/weka/airtesting_mujava.arff"));
	 * writer.write(relation.toString()); writer.flush(); writer.close(); }
	 * catch (IOException e) { // TODO Auto-generated catch block
	 * e.printStackTrace(); }
	 * 
	 * }
	 */

}
