package airtesting.wekautil.clustering;

/*import weka.clusterers.ClusterEvaluation;
import weka.clusterers.EM;
import weka.core.Instances;
import weka.core.converters.ConverterUtils.DataSource;
import weka.filters.Filter;
import weka.filters.unsupervised.attribute.Remove;*/

public class GroupingEM {
	public static void main(String[] args) {
		/*
		 * try{ String[] flores = {"Iris-virginicae", "Iris-virginica",
		 * "Iris-versicolor", "Iris-setosa", "Iris-setosa", "Iris-setosa",
		 * "Iris-virginica", "Iris-virginica", "Iris-virginica", "Iris-setosa",
		 * "Iris-versicolor" , "Iris-versicolor", "Iris-versicolor",
		 * "Iris-versicolor", "Iris-virginica"}; // load data /* Instances data
		 * = DataSource.read(flores[0]); data.setClassIndex(data.numAttributes()
		 * - 1);
		 * 
		 * // generate data for clusterer (w/o class) Remove filter = new
		 * Remove(); filter.setAttributeIndices("" + (data.classIndex() + 1));
		 * filter.setInputFormat(data); Instances dataClusterer =
		 * Filter.useFilter(data, filter);
		 * 
		 * // train clusterer EM clusterer = new EM(); // set further options
		 * for EM, if necessary... clusterer.buildClusterer(dataClusterer);
		 * 
		 * // evaluate clusterer ClusterEvaluation eval = new
		 * ClusterEvaluation(); eval.setClusterer(clusterer);
		 * eval.evaluateClusterer(data);
		 * 
		 * // print results System.out.println(eval.clusterResultsToString());
		 * 
		 * } catch (Exception e) { // TODO Auto-generated catch block
		 * e.printStackTrace(); }
		 */
	}

}
